name: Sync Upstream
run-name: "Sync from latest upstream tag at ${{ github.event.inputs.upstream_repo || 'https://github.com/Notsfsssf/pixez-flutter.git' }}"

on:
  schedule:
    - cron: "0 0 * * *" # This runs the action every day at midnight (UTC)
  workflow_dispatch:
    inputs:
      upstream_repo:
        description: "Upstream repo URL"
        required: false
        default: "https://github.com/Notsfsssf/pixez-flutter.git"

env:
  pull_request_state: ""
  upstream_repo: ${{ github.event.inputs.upstream_repo || 'https://github.com/Notsfsssf/pixez-flutter.git' }}

jobs:
  sync_upstream:
    name: Sync Upstream
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT }}

      - name: Set up Git committer identity
        run: |
          git config --global user.name "Sync Upstream Action"
          git config --global user.email "19920697+sync-upstream-action@users.noreply.github.com"
          git config --global push.autoSetupRemote true
          git checkout main
          git pull

      - name: Validate upstream repo
        run: |
          if ! git ls-remote --heads --tags "${{ env.upstream_repo }}" > /dev/null; then
            echo "Invalid upstream repo: ${{ env.upstream_repo }}"
            exit 1
          fi
        shell: bash

      - name: Check if sync_upstream branch exists
        id: check_branch
        run: |
          output=$(git ls-remote --heads origin refs/heads/sync_upstream | wc -l)
          if [output = "1"]; then
            echo "sync_upstream branch exists"
          else
            echo "sync_upstream branch does not exist"
            git branch sync_upstream
            git push origin sync_upstream
          fi
          git pull
        continue-on-error: true
        shell: bash

      - name: Merge tags/latest into sync_upstream
        run: | 
          git remote add -f upstream "${{ env.upstream_repo }}"
          git remote -v
          git branch --all
          git config --list
          git checkout sync_upstream
          git config pull.rebase false
          git pull origin sync_upstream
          git fetch upstream "refs/tags/*:refs/tags/upstream/*"
          latest_upstream_tag=$(git for-each-ref --sort=-creatordate --format='%(refname:short)' refs/tags/upstream | head -n 1)
          git merge --allow-unrelated-histories -X theirs "$latest_upstream_tag"
        shell: bash
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.PAT }}
          branch: sync_upstream

      # - name: Check for changes between branches
      #   id: check_changes
      #   run: |
      #     changes=$(git diff main sync_upstream)
      #     if [ -z "$changes" ]; then
      #       echo "No new changes found. Exiting..."
      #       exit 1
      #     fi
      #   shell: bash

      - name: Push changes to the new branch
        run: |
          git push

      - name: Check existing pull requests
        id: check_pr
        run: |
          pr=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}/pulls?head=${{ github.repository_owner  }}:sync_upstream" | jq -r '.[0].state')
          echo "pull_request_state=$pr" >> $GITHUB_ENV
          echo "pull_request_state=$pr"
        shell: bash

      - name: Checkout main
        run: |
          git checkout main

      - name: Reset sync_upstream branch
        run: |
          git fetch origin sync_upstream:sync_upstream --depth=1
          git reset --hard sync_upstream

      - name: Create pull request if not exists or closed
        if: env.pull_request_state == 'closed' || env.pull_request_state == 'null'
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.PAT }}
          commit-message: "Merge changes from upstream"
          title: "Sync Upstream"
          body: "This pull request was automatically generated by a daily GitHub Action to merge changes from upstream."
          branch: sync_upstream
          base: main

      - name: Check existing pull requests again
        id: check_pr_2
        run: |
          pr=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}/pulls?head=${{ github.repository_owner  }}:sync_upstream" | jq -r '.[0].state')
          echo "pull_request_state=$pr" >> $GITHUB_ENV
          echo "pull_request_state=$pr"
        shell: bash

      - name: Delete branch if pull request is not created
        if: env.pull_request_state == 'closed' || env.pull_request_state == 'null'
        run: git push origin --delete sync_upstream
