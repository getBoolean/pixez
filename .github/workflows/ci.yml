name: CI
on:
  workflow_dispatch:
  push:
    branches:
      - "**"
      - "!gh-pages"
    tags:
      - "v*"

env:
  FLUTTER_CHANNEL: "stable"
  FLUTTER_VERSION: "3.38.9"
  BRANCH_NAME: $(echo -n ${{ github.head_ref || github.ref_name }} | tr "/" "-")

permissions:
  contents: write

jobs:
  build_ios_without_signing:
    name: Build Flutter for iOS (Without Signing)
    runs-on: macos-15
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set Variables
        run: |
          echo "BRANCH_NAME=${{ env.BRANCH_NAME }}" >> $GITHUB_ENV
          echo "BRANCH_NAME=${{ env.BRANCH_NAME }}"

      - name: Install and Setup Flutter version
        uses: subosito/flutter-action@v2
        with:
          channel: ${{env.FLUTTER_CHANNEL}}
          flutter-version: ${{env.FLUTTER_VERSION}}
          cache: true
          cache-key: "flutter-:os:-:channel:-:version:-:arch:-:hash:"
          cache-path: "${{ runner.tool_cache }}/flutter/:channel:-:version:-:arch:"

      - name: Check Flutter Version
        run: flutter --version

      - name: Get Packages
        run: flutter pub get

      - name: Generate Code
        run: dart run build_runner build --delete-conflicting-outputs

      # Zip into IPA from here:
      # https://gist.github.com/devhammed/617d920b7ee886591a46c22633ab0093
      - name: Build Flutter IPA
        run: |
          cd ios && pod deintegrate && pod install && cd ..
          flutter build ios --release --no-codesign
          cd build/ios/iphoneos
          mkdir Payload
          cd Payload
          ln -s ../Runner.app
          cd ..
          zip -r app-release.ipa Payload
          mv app-release.ipa pixez-${{ env.BRANCH_NAME }}-ios.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: pixez-${{ env.BRANCH_NAME }}-ios
          path: build/ios/iphoneos/pixez-*.ipa

      - name: Release IPA
        if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags/v')
        uses: ncipollo/release-action@v1
        with:
          artifacts: build/ios/iphoneos/pixez-*.ipa
          allowUpdates: true
          omitBodyDuringUpdate: true

      - name: Read release notes from tag annotation
        id: release_notes
        if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags/v')
        run: |
          notes="$(git tag -l --format='%(contents)' "${{ github.ref_name }}")"
          delimiter="$(openssl rand -hex 8)"
          {
            echo "text<<${delimiter}"
            echo "${notes}"
            echo "${delimiter}"
          } >> "$GITHUB_OUTPUT"

      - name: Update AltStore metadata
        if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags/v')
        run: |
          ipa_filename="pixez-${{ env.BRANCH_NAME }}-ios.ipa"
          ipa_path="build/ios/iphoneos/${ipa_filename}"
          version="${GITHUB_REF_NAME#v}"
          version_date="$(date -u +"%Y-%m-%dT%H:%M:%S")"
          size="$(stat -f%z "$ipa_path")"
          notes="${{ steps.release_notes.outputs.text }}"
          if [ -z "$notes" ]; then
            notes="Release ${GITHUB_REF_NAME}"
          fi
          download_url="https://github.com/${{ github.repository }}/releases/download/${GITHUB_REF_NAME}/${ipa_filename}"

          jq '.apps[0].versions[0].size = '"$size"'' AltStore.json > /tmp/altstore_tmp.json && mv /tmp/altstore_tmp.json AltStore.json
          jq --arg desc "$notes" '.apps[0].versions[0].localizedDescription = $desc' AltStore.json > /tmp/altstore_tmp.json && mv /tmp/altstore_tmp.json AltStore.json
          jq --arg date "$version_date" '.apps[0].versions[0].date = $date' AltStore.json > /tmp/altstore_tmp.json && mv /tmp/altstore_tmp.json AltStore.json
          jq --arg ver "$version" '.apps[0].versions[0].version = $ver' AltStore.json > /tmp/altstore_tmp.json && mv /tmp/altstore_tmp.json AltStore.json
          jq --arg min "13.0" '.apps[0].versions[0].minOSVersion = $min' AltStore.json > /tmp/altstore_tmp.json && mv /tmp/altstore_tmp.json AltStore.json
          jq --arg url "$download_url" '.apps[0].versions[0].downloadURL = $url' AltStore.json > /tmp/altstore_tmp.json && mv /tmp/altstore_tmp.json AltStore.json

      - name: Commit AltStore metadata update
        if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags/v')
        run: |
          cp AltStore.json /tmp/AltStore.json
          git fetch origin main
          git checkout -B main origin/main
          cp /tmp/AltStore.json AltStore.json

          git add AltStore.json
          if git diff --cached --quiet; then
            echo "No AltStore changes detected."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: update AltStore metadata for ${{ github.ref_name }}"
          git push origin HEAD:main

  build_android:
    name: Build Flutter for Android
    runs-on: macos-15
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set Variables
        run: |
          echo "BRANCH_NAME=${{ env.BRANCH_NAME }}" >> $GITHUB_ENV
          echo "BRANCH_NAME=${{ env.BRANCH_NAME }}"

      - name: Install Java JDK
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Check Java Version
        run: |
          java --version
          which java

      - name: Install and set Flutter version
        uses: subosito/flutter-action@v2
        with:
          channel: ${{env.FLUTTER_CHANNEL}}
          flutter-version: ${{env.FLUTTER_VERSION}}
          cache: true
          cache-key: "flutter-:os:-:channel:-:version:-:arch:-:hash:"
          cache-path: "${{ runner.tool_cache }}/flutter/:channel:-:version:-:arch:"

      - name: Check Flutter Version
        run: flutter --version

      - name: Get Packages
        run: flutter pub get

      - name: Generate Code
        run: flutter pub run build_runner build --delete-conflicting-outputs

      # - name: Build Android Split APK
      #   run: |
      #     flutter pub get
      #     flutter build apk --release --split-per-abi
      #     cd build/app/outputs/flutter-apk
      #     mv app-arm64-v8a-release.apk pixez-${{ env.BRANCH_NAME }}-android-arm64-v8a.apk
      #     mv app-armeabi-v7a-release.apk pixez-${{ env.BRANCH_NAME }}-android-armeabi-v7a.apk
      #     mv app-x86_64-release.apk pixez-${{ env.BRANCH_NAME }}-android-x86_64.apk

      # - name: Upload Split APKs
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: pixez-${{ env.BRANCH_NAME }}-android-split
      #     path: build/app/outputs/flutter-apk/pixez-*.apk

      # - name: Release Split APKs
      #   if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags/v')
      #   uses: ncipollo/release-action@v1
      #   with:
      #     artifacts: build/app/outputs/flutter-apk/pixez-*.apk
      #     allowUpdates: true
      #     omitBodyDuringUpdate: true

      - name: Build Universal APK
        run: |
          flutter pub get
          flutter build apk --release
          cd build/app/outputs/flutter-apk
          mv app-release.apk pixez-${{ env.BRANCH_NAME }}-android-universal.apk

      - name: Upload Universal APK
        uses: actions/upload-artifact@v4
        with:
          name: pixez-${{ env.BRANCH_NAME }}-android-universal
          path: build/app/outputs/flutter-apk/pixez-${{ env.BRANCH_NAME }}-android-universal.apk

      - name: Release Universal APKs
        if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags/v')
        uses: ncipollo/release-action@v1
        with:
          artifacts: build/app/outputs/flutter-apk/pixez-${{ env.BRANCH_NAME }}-android-universal.apk
          allowUpdates: true
          omitBodyDuringUpdate: true

      - name: Build Android App Bundle
        run: |
          flutter pub get
          flutter build appbundle --release
          cd build/app/outputs/bundle/release
          mv app-release.aab pixez-${{ env.BRANCH_NAME }}-android-appbundle.aab

      - name: Upload Android App Bundle
        uses: actions/upload-artifact@v4
        with:
          name: pixez-${{ env.BRANCH_NAME }}-android-appbundle
          path: build/app/outputs/bundle/release/*.aab

      - name: Release Android App Bundle
        if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags/v')
        uses: ncipollo/release-action@v1
        with:
          artifacts: build/app/outputs/bundle/release/pixez-*.aab
          allowUpdates: true
          omitBodyDuringUpdate: true

  # build_msix:
  #   name: Build Flutter (Windows MSIX)
  #   runs-on: windows-latest
  #   steps:
  #     - name: Checkout Repository
  #       uses: actions/checkout@v3
  #     - name: Set Variables
  #       run: |
  #         echo "BRANCH_NAME=${{ env.BRANCH_NAME }}" >> $GITHUB_ENV
  #         echo "BRANCH_NAME=${{ env.BRANCH_NAME }}"
  #     # - name: Setup SQLite environment
  #       # uses: ccorsi/setup-sqlite@v1.0.1
  #       # run: |
  #       #   $url = "https://www.sqlite.org/2023/sqlite-dll-win64-x64-3420000.zip"
  #       #   $zip = "$env:TEMP\sqlite-dll-win64-x64-3420000.zip"
  #       #   $dest = "$env:TEMP\sqlite-dll-win64-x64-3420000"
  #       #   Invoke-WebRequest -Uri $url -OutFile $zip
  #       #   Expand-Archive -Path $zip -DestinationPath $dest
  #       #   echo "SQLITE3=$dest\sqlite3.dll" >> $env:GITHUB_ENV
  #     - name: Check SQLite environment
  #       run: |
  #         echo $env:SQLITE3
  #     - name: Setup Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         channel: "stable"
  #     - run: flutter clean
  #     - run: flutter pub get
  #     - run: dart run build_runner build --delete-conflicting-outputs
  #     - name: Build Windows
  #       run: flutter build windows --release
  #     - name: Package as Windows MSIX
  #       run: dart run msix:create
  #     - name: Upload msix
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: windows_msix
  #         path: build/windows/runner/Release/*.msix
